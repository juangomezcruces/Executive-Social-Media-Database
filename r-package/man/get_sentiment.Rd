% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core.R
\name{get_sentiment}
\alias{get_sentiment}
\title{Get sentiment-classified tweets}
\usage{
get_sentiment(leader = NULL, start = NULL, end = NULL)
}
\arguments{
\item{leader}{A `leader_id`, full name, X handle or unique surname,
matched case-insensitively. `NULL` (the default) returns every leader.}

\item{start, end}{Inclusive `"YYYY-MM-DD"` bounds on `created_at`. `NULL`
leaves that end of the range open.}
}
\value{
A tibble with one row per classified tweet, carrying the predicted
  label and the three class probabilities.
}
\description{
Covers the 11 leaders that were run through the sentiment classifier. The
sentiment table carries no timestamp of its own, so it is joined to the
tweets table on `tweet_uid` before the date filter is applied.
}
\examples{
\dontrun{
get_sentiment("Trudeau")
}
}
\seealso{
[get_tweets()]
}
